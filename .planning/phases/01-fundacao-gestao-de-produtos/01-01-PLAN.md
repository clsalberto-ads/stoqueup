---
phase: 01-fundacao-gestao-de-produtos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, package.json, drizzle.config.ts, src/db/index.ts, src/lib/auth.ts, .env.example]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Sistema inicializa via Docker Compose"
    - "Conexão com o banco de dados PostgreSQL estabelecida"
    - "Better Auth inicializado com plugin Admin e Organization"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL e ambiente local"
      contains: "postgres:16"
    - path: "src/db/index.ts"
      provides: "Drizzle ORM client"
      contains: "drizzle"
    - path: "src/lib/auth.ts"
      provides: "Configuração do Better Auth"
      contains: "betterAuth"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/db/index.ts"
      via: "Database adapter"
---

<objective>
Estabelecer a infraestrutura básica do projeto StoqueUp, incluindo ambiente Docker, banco de dados PostgreSQL, Drizzle ORM e a base do sistema de autenticação via Better Auth.

Purpose: Garantir que o projeto tenha um ambiente de desenvolvimento sólido e um sistema de identidade funcional.
Output: Arquivos de configuração de infra, cliente de banco de dados e inicialização do Better Auth.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infraestrutura Docker e Next.js</name>
  <files>docker-compose.yml, package.json, .env.example, next.config.ts</files>
  <action>
    1. Criar `docker-compose.yml` com serviço `db` (Postgres 16) e `app` (Next.js).
    2. Configurar `package.json` com scripts básicos (`dev`, `db:push`, `db:studio`).
    3. Criar `.env.example` com `DATABASE_URL`, `BETTER_AUTH_SECRET`, e `NEXT_PUBLIC_APP_URL`.
    4. Garantir que o Next.js 16 esteja com App Router e TypeScript ativos.
  </action>
  <verify>docker compose ps</verify>
  <acceptance_criteria>
    - docker-compose.yml contém serviço de banco de dados.
    - package.json contém scripts para Drizzle.
    - .env.example está presente e documentado.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Setup Drizzle ORM e Schema Base</name>
  <files>drizzle.config.ts, src/db/index.ts, src/db/schema.ts</files>
  <action>
    1. Configurar `drizzle.config.ts` apontando para o dialeto postgres.
    2. Criar cliente `db` em `src/db/index.ts`.
    3. Definir schema inicial em `src/db/schema.ts` incluindo as tabelas padrão exigidas pelo Better Auth (user, session, account).
  </action>
  <verify>npx drizzle-kit check</verify>
  <acceptance_criteria>
    - drizzle.config.ts existe e está configurado.
    - src/db/index.ts exporta o cliente db.
    - src/db/schema.ts contém tabelas de usuários.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Inicialização Better Auth</name>
  <files>src/lib/auth.ts, src/app/api/auth/[...better-auth]/route.ts</files>
  <action>
    1. Configurar `betterAuth` em `src/lib/auth.ts` utilizando o adapter do Drizzle.
    2. Adicionar os plugins `admin()` e `organization()`.
    3. Criar a rota de API do Next.js para lidar com as requisições de autenticação.
  </action>
  <verify>ls src/app/api/auth/\[...better-auth\]/route.ts</verify>
  <acceptance_criteria>
    - src/lib/auth.ts importa e configura admin e organization plugins.
    - Rota de API do Better Auth está criada.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Database Schema Push</name>
  <action>Executar `npx drizzle-kit push` para sincronizar o schema inicial com o banco de dados Docker.</action>
  <verify>npx drizzle-kit introspect (ou conferir via Drizzle Studio)</verify>
  <acceptance_criteria>
    - Comando retorna sucesso na sincronização.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] docker compose up -d executa sem erros.
- [ ] npm run dev inicia o Next.js com sucesso.
- [ ] Drizzle ORM consegue se conectar ao banco de dados local.
</verification>

<success_criteria>
- Ambiente de desenvolvimento dockerizado pronto.
- Better Auth configurado e pronto para uso de roles e organizações.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-gestao-de-produtos/01-01-SUMMARY.md`
</output>
