---
phase: 02
plan: 02
title: Lógica de Vendas e Gatilhos
slug: 02-02-logica
wave: 2
depends_on: [02-01-infra]
files_modified: [src/lib/inventory-actions.ts]
requirements: [SALE-01, SALE-02, SALE-03, PROD-01]
autonomous: true
---

# Plan: 02-02-LOGICA

Implementação da lógica de negócio transacional para vendas e automação de gatilhos de produção.

## Tasks

<task id="1" type="execute">
<read_first>
- src/db/schema.ts
- src/lib/auth-actions.ts (referência de padrão)
</read_first>
<action>
Criar o arquivo `src/lib/inventory-actions.ts` com a Server Action `sellProduct`.
A action deve:
1. Receber `productId` e `quantity`.
2. Verificar autenticação do usuário.
3. Iniciar uma transação `db.transaction`.
4. Atualizar o estoque de forma atômica: `currentStock = currentStock - quantity` onde `id = productId` e `currentStock >= quantity`.
5. Se o update falhar (estoque insuficiente), lançar erro.
6. Inserir log em `inventoryLogs` com tipo `SALE`.
7. Verificar se `currentStock <= qtdMinima`.
8. Se sim, e não houver tarefa `PENDING` para o produto, inserir nova tarefa em `productionTasks`.
9. Retornar sucesso.
</action>
<acceptance_criteria>
- `src/lib/inventory-actions.ts` exporta `sellProduct`.
- A função utiliza `db.transaction`.
- Existe verificação de existência de tarefa pendente antes de criar nova.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- Criar script temporário para testar a action e verificar decremento e criação de tarefa.

### Manual Verification (UAT)
- [ ] Chamar `sellProduct` via console ou componente e verificar no banco:
  - O estoque diminuiu?
  - O log foi criado?
  - Se caiu abaixo do mínimo, a tarefa de produção foi gerada?
