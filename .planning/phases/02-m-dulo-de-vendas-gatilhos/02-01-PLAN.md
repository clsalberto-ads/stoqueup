---
phase: 02
plan: 01
title: Infraestrutura e Notificações
slug: 02-01-infra
wave: 1
depends_on: []
files_modified: [src/db/schema.ts, src/app/layout.tsx]
requirements: [PROD-01]
autonomous: true
---

# Plan: 02-01-INFRA

Este plano estabelece a base de dados necessária para tarefas de produção e configura o sistema de notificações (Toast) para feedback ao usuário.

## Tasks

<task id="1" type="execute">
<read_first>
- src/db/schema.ts
</read_first>
<action>
Adicionar a tabela `production_tasks` ao arquivo `src/db/schema.ts`.
A tabela deve conter:
- `id`: text, primary key
- `productId`: text, references products.id
- `status`: text, default 'PENDING' (valores: PENDING, IN_PROGRESS, COMPLETED)
- `quantity`: integer (opcional, ou baseado no lote padrão)
- `createdAt`: timestamp
- `updatedAt`: timestamp
</action>
<acceptance_criteria>
- src/db/schema.ts contém `export const productionTasks = pgTable("production_tasks", ...)`
- O comando `npx drizzle-kit push` é executado com sucesso após a alteração.
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- package.json
</read_first>
<action>
Instalar a biblioteca `sonner` para notificações.
</action>
<acceptance_criteria>
- package.json contém `sonner` nas dependências.
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- src/app/layout.tsx
</read_first>
<action>
Adicionar o componente `<Toaster />` da biblioteca `sonner` ao layout raiz da aplicação para permitir disparar notificações de qualquer lugar.
</action>
<acceptance_criteria>
- src/app/layout.tsx importa e renderiza `<Toaster />`.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- N/A (Infra e UI Setup)

### Manual Verification (UAT)
- [ ] Verificar se o banco de dados reflete a nova tabela `production_tasks`.
- [ ] Verificar se um `toast("Teste")` simples aparece na tela (pode ser testado temporariamente).
