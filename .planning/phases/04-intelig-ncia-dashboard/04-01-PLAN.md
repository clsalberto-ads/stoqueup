---
phase: 04
plan: 01
title: Setup de Tailwind e Tremor
slug: 04-01-setup
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.ts, postcss.config.mjs, src/app/globals.css]
requirements: [DASH-01]
autonomous: true
---

# Plan: 04-01-SETUP

Configuração do ambiente para suportar componentes modernos de dashboard.

## Tasks

<task id="1" type="execute">
<read_first>
- package.json
</read_first>
<action>
Instalar as dependências de estilo:
`npm install -D tailwindcss postcss autoprefixer`
`npm install @tremor/react @headlessui/react @tailwindcss/forms @remixicon/react`
</action>
<acceptance_criteria>
- Dependências listadas no `package.json`.
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- None
</read_first>
<action>
Criar o arquivo `tailwind.config.ts` na raiz do projeto com as configurações para o Tremor (incluindo o path `node_modules/@tremor/...` no content).
</action>
<acceptance_criteria>
- Arquivo criado e exportando a configuração correta.
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- None
</read_first>
<action>
Criar o arquivo `postcss.config.mjs` para processar o Tailwind.
</action>
<acceptance_criteria>
- Arquivo criado com `tailwindcss` e `autoprefixer`.
</acceptance_criteria>
</task>

<task id="4" type="execute">
<read_first>
- src/app/globals.css
</read_first>
<action>
Adicionar as diretivas `@tailwind` no topo do arquivo `src/app/globals.css`.
</action>
<acceptance_criteria>
- `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;` presentes no arquivo.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- Executar `npm run dev` e verificar se não há erros de compilação do PostCSS.

### Manual Verification (UAT)
- [ ] Aplicar uma classe do tailwind (ex: `text-red-500`) temporariamente em um elemento e ver se funciona.
